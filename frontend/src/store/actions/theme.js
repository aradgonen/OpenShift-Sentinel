import { CHANGE_THEME } from "./theme-types";

// export const changeTheme = (themeMode) => (dispatch) => {
//     return { dispatch({
//         type: CHANGE_THEME,
//         payload: themeMode,
//     })}
// }